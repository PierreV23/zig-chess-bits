{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run",
            "type": "shell",
            // "command": "zig build run -Dpath='${file}' -- '${fileDirname}' '${file}'",
            "command": "zig run main.zig",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$gcc"
            ]
        }
    ]
}